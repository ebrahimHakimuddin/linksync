{
  "name": "linksync",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --recursive build",
    "build:extension": "pnpm --filter @linksync/extension build",
    "dev:server": "pnpm --filter @linksync/server dev",
    "test": "pnpm --recursive test",
    "typecheck": "pnpm --recursive typecheck"
  }
}
